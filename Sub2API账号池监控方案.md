# Sub2API 账号池监控方案

更新时间：2026-08-06

## 目标

在 VibeToken 菜单栏弹框内展示 Sub2API 管理后台 OpenAI OAuth 实体账号池的 5h、7d 用量窗口剩余容量，让管理员无需逐个打开账号检查窗口状态。

本功能监控的是上游账号池，不是中转用户余额、API Key 配额、单个账号余额或本地 Codex 会话费用。

## 数据契约

实现依据为 Sub2API 官方仓库当前管理员接口：

- 管理员登录：`POST /api/v1/auth/login`
- 两步验证：`POST /api/v1/auth/login/2fa`
- 令牌刷新：`POST /api/v1/auth/refresh`
- 账号分页：`GET /api/v1/admin/accounts`

账号分页请求限定 `platform=openai&type=oauth`，只读取账号状态、调度状态、母子关系、计划类型和 Codex 窗口快照。实现同时兼容 canonical 5h/7d 字段和按窗口分钟数识别的 legacy primary/secondary 字段。

上游 `credentials` 与 `extra` 是动态字典，同一语义字段可能以 JSON 数字或数字字符串返回，时间还可能使用 ISO8601、Unix 秒或毫秒时间戳。客户端对这些等价表示做兼容；可选动态块类型异常时忽略该块并继续处理同页账号。账号 ID、状态和可调度性保持严格解码，任何缺失或类型异常都会拒绝该页，防止错误聚合。

## 聚合口径

单账号窗口剩余比例：

```text
remaining = clamp(1 - used_percent / 100, 0, 1)
```

账号池窗口等效剩余容量：

```text
remaining_equivalent_accounts = sum(remaining)
remaining_fraction = remaining_equivalent_accounts / observed_accounts
```

5h 与 7d 独立计算。不同计划分别分组，主界面同时展示全池等效容量。该值不能解释为绝对 Token、请求数或美元余额。

实际可用口径：

```text
account_effective_remaining = min(five_hour_remaining, seven_day_remaining)
pool_effective_remaining = sum(account_effective_remaining)
pool_effective_fraction = pool_effective_remaining / assessable_accounts
displayed_remaining_percent = pool_effective_remaining * 100%
displayed_total_percent = assessable_accounts * 100%
```

账号必须同时满足状态正常、参与调度、快照未过期、两个窗口均已探测且剩余比例都大于 0，才计入“当前可用”。任一窗口为 0 时计入“窗口受限”，即使另一个窗口仍为 100% 也不能视为可用。缺失或过期窗口不进入实际可用分母，单独列为数据异常。

界面分别展示 `available_accounts / assessable_accounts` 与全池累计“剩余额度 / 总额度”。每个满额账号代表 `100%`；一个账号剩余 77%、其余 10 个账号受限时显示 `1 / 11` 与 `77% / 1,100%`，进度条为全池剩余 `7%`。窗口明细分别展示 5h、7d 的累计剩余百分比，便于直接判断限制窗口。两个账号分别实际可用 80% 和 60% 时，剩余额度显示 `140%`，不取平均值。缺失或过期窗口不进入已探测分母，并继续单列为数据异常。

## 纳入与排除

纳入：

- OpenAI OAuth 实体账号。
- `status=active`。
- `schedulable=true`。

排除或单列：

- `parent_account_id` 非空的 Spark 影子账号不参与求和。
- 禁用、错误或不可调度账号计入“不可用”。
- 5h 或 7d 任一窗口缺失的账号计入“未探测”。
- 快照更新时间超过 15 分钟的账号计入“数据过期”；保留原始窗口信息，但不计入当前可用。
- 两个窗口数据有效但任一窗口已耗尽的账号计入“窗口受限”。

## 刷新策略

- 本地 Codex Token 继续由文件事件实时刷新。
- 首次连接成功后保存本地会话；应用启动时自动恢复连接并同步中转账号池。
- 未保存会话时保持未连接，本地 Token 刷新不会发起 Sub2API 请求。
- Sub2API 联网读取最短间隔 30 秒，避免实时模式每 5 秒访问后台。
- 手动刷新绕过本地 30 秒缓存，立即重新读取账号列表。
- 单次请求超时 15 秒；每页 100 个账号；最多 100 页。
- 当前不调用 `source=active&force=true`，不批量触发上游探测。

## 安全边界

- 远程地址必须使用 HTTPS；仅本机回环地址允许 HTTP。
- 不接受带内嵌用户名或密码的 URL。
- 密码只用于当次登录，提交后立即从界面状态清空。
- 服务地址、管理员邮箱、Access Token、Refresh Token 和过期时间分别写入应用支持目录中的本地 JSON 文件；管理员密码不保存。
- 应用不读取、迁移或写入 macOS Keychain，不会触发钥匙串授权框。
- 应用支持目录权限为 `700`，会话与连接文件权限为 `600`；保存采用原子替换，读取会拒绝符号链接、非普通文件、损坏 JSON 和超过 64 KB 的文件。
- 本地文件保护弱于 Keychain；同一 macOS 用户权限下运行的其他程序理论上可以读取登录令牌。
- 后台响应使用临时网络会话读取，禁用磁盘缓存与 Cookie 持久化。
- TOTP 临时令牌只保存在进程内存中。
- 日志只记录脱敏错误类型，不记录地址、账号、密码、令牌或响应正文。
- 所有数据接口均为只读，不修改、重置或删除 Sub2API 账号。

## 界面状态

- 未连接：显示连接入口。
- 配置入口：在当前菜单 popover 内显示模态层，不创建附属 sheet；表单可直接交互，点击遮罩、关闭按钮或 Esc 返回。
- 连接中/同步中：显示进度反馈并阻止重复提交。
- 需要两步验证：显示 6 位验证码输入与重新登录入口。
- 已连接：左侧显示当前可用账号数与已探测账号数；右侧按每个账号 `100%` 显示全池剩余额度 / 总额度、累计 5h/7d 剩余和限制窗口。
- 诊断数量：互斥展示当前可用、窗口受限、不可用和数据异常；过期与未探测数量在辅助信息中分别列出。
- 网络失败或登录失效：保留最后成功快照，并明确显示错误。
- 断开连接：二次确认后清除本地会话/连接文件和当前缓存。

## 验收标准

- 分页账号全部读取，且存在安全页数上限。
- Spark 影子账号不会重复计数。
- 百分比越界被限制在 0% 至 100%。
- 5h、7d 和不同计划不发生口径串用。
- 任一窗口耗尽时，该账号的实际可用额度为 0，不会因另一窗口仍有余额而显示为可用。
- 账号数量与额度使用同一个已探测账号池边界；额度以账号等效容量相加并同时显示分子、分母，不把平均值伪装成单账号余额。
- 401 最多自动刷新令牌并重试一次，不出现无限重试。
- 单个账号的可选动态块类型异常不会导致整个账号分页解析失败。
- 必填账号身份与状态字段异常时不会静默纳入聚合。
- 中转故障不影响本地 Codex Token 统计。
- 首次连接后重启应用可自动恢复，无需重复填写服务地址、邮箱或密码。
- 应用运行期间不会读取或写入 macOS Keychain，也不会触发钥匙串授权框。
- 本地配置文件权限、文件类型、大小限制、损坏内容和删除行为均有自动测试。
- 中英文 500×720 菜单窗口无文字重叠、截断或布局跳动。
- 源码、测试、README、开发进度与本方案同步。
