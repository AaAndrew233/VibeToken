# VibeToken 开发进度

更新时间：2026-08-06

## 当前已完成

- 原生 macOS 14+ SwiftUI/AppKit 工程。
- `NSStatusItem` 菜单栏入口与完整原生浮层；Token、费用、分类、模型/工具分布、来源、当前会话和刷新设置均在同一弹框内完成，不再打开独立详情窗口；点击弹框外区域会自动关闭，并在关闭后清理临时事件监听。
- Apple-inspired 中文界面与英文切换。
- Sub2API 管理员账号池监控：管理员登录、TOTP 两步验证、Access Token 自动刷新、本地会话持久化和断开连接二次确认。
- Sub2API 配置页改为菜单 popover 内的模态层，避免 `NSPopover` 附属 sheet 无法获得点击焦点；支持遮罩、关闭按钮和 Esc 返回。
- 完全移除 Sub2API Keychain 读写；首次连接后自动保存服务地址、邮箱和会话 Token，后续启动自动恢复连接，不再弹出钥匙串授权框。管理员密码始终不落盘。
- Sub2API 本地配置目录使用 `700`、文件使用 `600` 权限和原子写入，并拒绝符号链接、非普通文件、损坏 JSON 与超过 64 KB 的文件。
- Sub2API OpenAI OAuth 账号分页只读采集；兼容 canonical 与 legacy Codex 5h/7d 窗口字段、数字字符串、ISO8601 与 Unix 秒/毫秒时间戳，并隔离异常的可选动态字段块。
- 5h/7d 账号等效剩余额度、重置时间、可调度/不可用/过期/未探测数量和计划分组展示。
- Sub2API 额度展示将账号数量与额度拆开：左侧显示当前可用账号数，例如 `1 / 11`；右侧显示全池“剩余额度 / 总额度”，例如 `74% / 1,100%`，并分别展示 5h、7d 的累计剩余额度。每个满额账号记为 `100%`，多个账号直接相加，不取平均值。
- 账号诊断改为互斥的当前可用、窗口受限、不可用和数据异常，缺失或过期窗口不会被误报为可用。
- Sub2API 计划分布与同步时间合并为同一行；仅在存在影子账号、数据过期或未探测时增加异常信息行。
- Spark 影子账号按 `parent_account_id` 排除，防止共享母账号额度被重复计入。
- 中转同步最短 30 秒节流、15 秒请求超时、最多 100 页安全上限和 15 分钟快照过期标记；手动刷新可立即同步。
- Codex Desktop / CLI 最近 30 天全部会话的真实 Token 增量读取。
- 今天、滚动 24H、7D、30D 四档时间范围；主数字按所选范围汇总全部 Codex 会话。
- Codex 当日最多 64 个活跃会话文件事件驱动刷新，约 100ms 防抖，5 秒低频轮询兜底。
- 刷新方式支持实时、每 5 分钟、每 30 分钟和手动，并持久化用户选择；底部显示精确到秒的最近同步时间，手动刷新具备同步中、成功和失败反馈，并在执行期间禁用重复点击。
- Token 数字在真实值之间采用约 1.2 秒、30fps 的进度型滚动，并遵循系统“减少动态效果”设置。
- 菜单栏缩小版同时显示 `Token｜预计花费`；已按 VibeCafé 公式与价格快照估算美元费用，并参考 Vibe Usage 只累计已定价模型，使 7D/30D 存在少量未知模型时仍可即时显示费用。
- 浮层显示定价 Token 覆盖率；范围内没有任何可定价模型时费用才显示 `--`。
- 输入、缓存、输出、推理和总 Token 分类。
- 用量趋势跟随时间范围：今天、24H 按小时聚合，7D、30D 按天聚合；支持 Token / 费用切换、空时间桶补零、悬停核对和部分定价提示。
- 浮层工具分布和模型分布采用 Vibe Usage 风格环形图；环形中心显示总量，右侧图例显示名称、紧凑数值和一位小数占比，悬停可联动突出；前 4 项后合并“其他”。
- Token 使用 `K / M / B` 紧凑格式，费用保留两位小数并在大额时使用 `K / M`，均不使用科学计数法。
- 数据源、当前会话、加载、无数据和错误状态均集成在浮层。
- SQLite WAL、v3 迁移、事件幂等写入、字节检查点和按时间聚合。
- 对齐 Vibe Usage 的 Codex 两层去重：累计总 Token 未变化的重复 emission 不重复计数；fork/subagent 通过父子元数据、原始 Token 序号和连续 payload 指纹移除 replay，支持父会话和子会话乱序入库。
- replay 去重只作用于有明确父子关系的会话；独立会话即使 Token 数或 payload 相同也不会误删。
- 脱敏日志、会话标识哈希和本地优先配置。
- 本地 `.app` 打包与 ad-hoc 签名脚本。

## 当前明确未完成

- 费用是 API 单价粗略估算，不是订阅账单；历史用量暂按当前价格快照回算，尚未保存历史价格版本。
- 升级前已写入 SQLite 的历史重复事件尚未自动重算；需要在用户确认后备份数据库并重建最近 30 天派生索引，原始 Codex 日志不动。
- Claude Code 及其余 18 类来源尚未实现。
- 尚未完成按项目聚合、30 天以前历史、全局价格覆盖率指标和数值化首次索引进度。
- 尚未完成 Developer ID 签名、Notarization、自动更新和公开分发。
- Sub2API 当前汇总的是百分比换算后的账号等效容量，不是上游未公开的绝对 Token 或美元配额。
- Sub2API 当前只接入 OpenAI OAuth/Codex 窗口；其他平台的额度结构不同，不能复用同一公式直接合并。
- 原生登录不绕过 Turnstile、腾讯验证码等浏览器验证码；启用验证码的实例仍需后续设计受支持的授权交接流程。
- 动态账号字段兼容已通过本地模拟响应测试；按用户安排，真实 Sub2API 账号池同步由用户自行验证。
- Sub2API 登录令牌改为普通本地配置文件后，保护强度低于 Keychain；同一 macOS 用户权限下运行的其他程序理论上可以读取。

## 已验证

- 自动化测试新增组合窗口有效额度、单窗口耗尽、缺失窗口和过期窗口用例；截图中的 11 个可调度容量、1 个当前可用、10 个窗口受限场景已通过定向聚合测试。2 项真实日志探针仍需显式开启，默认跳过。
- 本机 2.68 GiB、最近 30 天真实会话首次只读索引约 18.6–21.3 秒；后续刷新只读取检查点后的新增字节。
- Token 与费用在菜单栏、汇总、趋势和分布中均按 Vibe Usage 紧凑格式显示，不出现科学计数法。
- 菜单浮层可在单一滚动区域完成完整核对，不再依赖独立详情窗口。
- 中文与英文可即时切换；500×720 弹框下 Token / 费用环形分布、多模型图例和长英文文案均无重叠或截断。
- 手动刷新真实交互已验证：按钮显示进度并防止重复点击，成功后短暂显示绿色确认，再恢复最近同步时间。
- Sub2API 中英文连接态已在真实 500×720 窗口验收，5h/7d 余额、进度条、诊断数量、计划摘要、同步时间和英文长文案无重叠或截断。
- 正常驻留的 `top` 内存占用约 24–25 MB，`ps` RSS 约 86 MB；空闲 CPU 为 0.0%，增量刷新采样峰值约 2.3%，符合低于 100 MB 的阶段目标。
- 应用包 Info.plist、ad-hoc 签名和数据库表结构检查通过。

## 下一批开发

1. 增加价格目录自动更新、签名校验、覆盖率和历史价格版本。
2. 完成按项目聚合和数值化首次索引进度。
3. 实现 Claude Code 适配器。
4. 实现 Gemini CLI 与 OpenCode 适配器，验证 JSON、JSONL、SQLite 三类来源契约。
5. 设计启用浏览器验证码时的安全授权交接，并按平台分别扩展中转额度适配器。
